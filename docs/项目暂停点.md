# ⏸️ 项目暂停点（2026-09-19）

> 这个文件回答一个问题：**下次坐回电脑前，从哪继续？**
> 项目随时可以停，停了也不欠账 —— 这份文件就是"存档"。

## 一、现在在哪

| 项 | 状态 |
|---|---|
| 仓库 | `github.com/xiaoxian33/xiaoxian33.github.io` |
| 本地 | `D:\Java works\xiaoxian33.github.io` |
| 最后一次提交 | 见 `git log --oneline -1`（写在文档里会过期，用命令查更准） |
| 工作区 | 干净（没有半成品） |
| 前端 | ✅ 已上线 <https://xiaoxian33.github.io>（资源版本 `?v=20260917d`） |
| 后端 | ✅ 本机 `localhost:8080`，**开发模式**（`mvnw spring-boot:run`） |
| 数据库 | ✅ `site_db`（8 篇随笔 + 一份资料 + 4 张表：essay / profile / post / image） |

## 二、怎么把它重新跑起来（3 步）

1. **起 MySQL**：双击 `D:\DevEnv\04-scripts\一键启动MySQL.bat`
   ⚠️ 那个黑窗口**不能关**，关掉 = 数据库停（后端会报 `Connection refused`）
2. **起后端**：
   ```powershell
   cd "D:\Java works\xiaoxian33.github.io\server"
   .\mvnw.cmd spring-boot:run
   ```
   或者：双击仓库根目录的 `start-backend.bat`
3. **打开网站**：<https://xiaoxian33.github.io>（页脚显示「后端已连接」就对了）

## 三、已经验证过、可以信任的东西

- 打包：`mvnw -q -DskipTests package` → `target\site-server-0.0.1-SNAPSHOT.jar`（50MB）
- 上线方式启动：`java -jar` 在 6 秒内起来，**传图 / 发帖 / 读回 / 删除** 9 项验证全过
- 接口契约、字段规则、失败码：`docs/API.md`
- 每个文件干什么：`docs/代码结构.md`

## 四、下次继续时，从这里接

**主线：把网站真正上线（省钱版：国内轻量 + 备案）**

1. 买服务器：腾讯云轻量，**2核2G 或 4核4G**，地域在国内，买 1 年
   - ⚠️ **镜像必须选「系统镜像 → Ubuntu 24.04 LTS」**
   - 卡住点：OpenClaw 促销专场可能锁死镜像 → **买完在控制台「更多 → 重装系统」改成 Ubuntu**
2. 买域名 + 实名 + 提交备案（域名要在同一家买；提交完别干等）
3. 然后看 **`docs/上线手册.md` 第 3 步**开始做
   👉 **第 3 步之后我全程代打**：你粘命令、把输出发我

**上线时才做的两处代码改动（先记着，现在别动）**

- `assets/js/app.js` 的 `API_BASE` → 改成 `''`（同源），并 bump `index.html` 里的 `?v=`
- 10 个 `@CrossOrigin(origins = "*")` → 收紧成自己的域名

## 五、三条纪律（每次动手都适用）

1. 改了 `style.css` / `app.js` → **记得 bump `index.html` 里的 `?v=`**（否则浏览器用旧缓存）
2. 密码 / 私密配置 → **永远不进仓库**（`.gitignore` 已经拦了 `application-local.properties`、`server/uploads/`）
3. **提交 + 推送之后，才算"做完"**（你平时是从 GitHub 上看效果的）

## 六、想继续学习时的入口

| 想干什么 | 看哪个文件 |
|---|---|
| 盘点"我用过哪些专业技能" | `docs/技能觉醒录.md`（49 条 + 10 个动手练习） |
| 每天学了什么、踩过什么坑 | `docs/LEARNING-LOG.md` |
| 上线要做什么、卡住怎么办 | `docs/上线手册.md`（12 步 + 排错表） |
| 接口要什么字段 | `docs/API.md` |

## 七、还没做的（下次随便挑）

- [ ] 上线（主线，见第四节）
- [ ] 「做过的项目」那一块页面（纯文字 + GitHub 链接）
- [ ] 清理没人用的图片文件
- [ ] 令牌从内存搬进数据库（重启不掉线）
- [ ] AI 分身

---

**一句话**：项目停在一个干净、能用、能被验证的状态。
停下来不需要理由，回来的时候看这份文件就够了。
