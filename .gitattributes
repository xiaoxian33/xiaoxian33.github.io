# 统一换行符：仓库里一律 LF，避免 Windows / 云端来回转换
* text=auto eol=lf

# Windows 脚本必须 CRLF，否则双击可能出问题
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制素材不做转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
